# CoDE-Stop 复现进度

最后更新：2026-09-21。对应 [新手复现计划](REPRODUCTION_PLAN.md)。本文件根据用户每天提供的进度及核验信息更新，保留简短调整记录，不依赖固定自然日推进。

记录方式：当前Notion服务连接已核验，任务数据库尚未建立或迁移，本文件仍为进度主记录。Notion迁移并读回验收后，本文件改作带导出时间的交接快照；目标链接、主对话和切换日期届时填写。接入方案见 [交接文档第9节](REPRODUCTION_HANDOFF.md)。

## 当前状态

- 可投入时间：每天6–8小时，按6小时核心任务加0–2小时弹性安排。
- 当前位置：D1待用户执行与汇报；已讲解目录结构，不据此认定已经理解代码。
- 已有可核验准备：独立项目目录、固定上游提交、复现/预算说明；上游提交为 `b5081e7c2abe23bb1d19649421cc13522fee7c50`。
- 本项目GPU实验：尚无已核验的执行结果。当前服务器、显卡、开机时间和本轮额度均待实际部署时确认。
- 目标：先完成单模型指定子集的可信复现。正式计算时长在D6测速后修订。
- Notion：2026-09-21用户已安装插件，只读身份查询成功，连接已核验；本项目目标数据库、实际读写权限及任务创建尚未核验。默认由当前项目的新主对话统一维护，其他账号提供辅助建议。

## 阶段台账

状态可用：未开始 / 进行中 / 受阻 / 待核验 / 完成。阶段序号不是日期。证据填写文件、日志、结果或用户能解释的具体内容；不保存密钥或机器登录凭据。

| 阶段 | 任务 | 状态 | 已完成证据 | 剩余任务/阻塞 | 实际日期 |
| --- | --- | --- | --- | --- | --- |
| D1 | 最小代码流程与本地准备 | 未开始 | 已提供结构讲解；尚未收到阅读汇报 | 阅读普通生成调用链，整理部署信息 | 待安排 |
| D2 | 服务器环境与首条输出 | 未开始 | — | 依赖D1 | — |
| D3 | 生成、补答、判分与保存 | 未开始 | — | 依赖D2 | — |
| D4 | DEER单条轨迹 | 未开始 | — | 依赖D3 | — |
| D5 | CoDE与论文/代码差异 | 未开始 | — | 依赖D4 | — |
| D6 | 小批量、长序列、恢复和资源诊断 | 未开始 | — | 依赖D5 | — |
| D7 | 开发预演与配置冻结 | 未开始 | — | 依赖D6 | — |
| D8 | 正式基础生成与Vanilla | 未开始 | — | 依赖D7；运行窗口待测速 | — |
| D9 | 正式DEER与CoDE | 未开始 | — | 依赖D8；运行窗口待测速 | — |
| D10 | 统计与复现报告 | 未开始 | — | 依赖D9 | — |

## 下一次优先任务

1. 阅读 `method_step_by_step()` 和 `models.generate()`，解释题目到回答的转换；建议2小时。
2. 略读论文摘要/§3.1及 `inference.py` 主循环，写下三种方法和输入输出关系；建议1.5小时。
3. 检查本地项目/工具状态，整理服务器准备项并汇报疑问；建议2.5小时，另留0–2小时缓冲。

不要求重新创建已存在的目录，不在这一阶段启动完整基准。具体操作在开始D1时逐条说明。

## 用户每日汇报模板

不必写正式日报，能定位当前状态即可：

```text
日期 / 正在做的阶段：
今天实际投入：约 __ 小时
已完成：
能提供的输出或文件：
没完成 / 卡住的地方：
报错：运行的命令 + 完整错误末尾（去掉密钥等敏感内容）
我目前不理解的问题：
明天可投入：约 __ 小时
如需GPU：是否已开机、本轮可运行到几点、剩余额度
```

助手据此更新台账和下一次任务；用户只提供“未完成”时，先保留现有进度，再集中定位一个关键阻塞，不要求把全部工作重述一遍。

## 调整记录

| 日期 | 依据 | 调整 | 后续影响 |
| --- | --- | --- | --- |
| 2026-09-21 | 用户首次LLM复现，每天6–8小时，希望亲自操作并允许顺延 | 建立10阶段计划，另留2–4弹性工作日；当前从D1开始 | 不设置固定完成日期；D6实测后重排正式计算窗口 |
| 2026-09-21 | 用户要求交接文档加入Notion方案 | 明确一个主对话、稳定任务编号、日期顺延和记录切换方式 | 仅更新文档；D1仍待执行，Notion尚未迁移 |
| 2026-09-21 | 用户安装Notion插件后，只读身份查询成功 | 将连接状态更新为已核验；未执行目标数据库操作 | Markdown仍为进度主记录，D1状态不变 |

## 每日回复应覆盖的内容

已核验完成什么 → 剩余哪个问题 → 下一天最多三个目标及时间分配 → 哪些后续阶段顺延。只在拿到进度证据后更新实际完成项；代码同步结果以当次Git核验为准。
